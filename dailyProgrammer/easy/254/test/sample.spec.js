import { expect } from 'chai';

describe('AtbashCipher', () => {
  describe('sample', () => {
    it('adds to 2', () => {
      expect('hi').to.be.a('string');
    });
    it('adds to 3', () => {
      expect(1).to.be.a('number');
    });
  });

});
